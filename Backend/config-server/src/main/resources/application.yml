server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/AkashGuptaNitrr/E-Commerce
          searchPaths: config-repo
##          or
#          uri: https://github.com/AkashGuptaNitrr/E-Commerce/config-repo

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka