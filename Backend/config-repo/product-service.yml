server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://postgres-service:5432/product_service
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true